spring:
    datasource:
        initialize: true
        password: postgres
        url: jdbc:postgresql://localhost:5432/favorite
        username: postgres
    jpa:
        hibernate:
            ddl-auto: update
            show-sql: true
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
